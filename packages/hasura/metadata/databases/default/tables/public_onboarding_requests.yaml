table:
  schema: public
  name: onboarding_requests
select_permissions:
  - role: user
    permission:
      columns:
        - confirmed
        - created_at
        - data
        - id
        - reviewed
      filter: {}
