CREATE TABLE "public"."github_usernames" ("user_id" integer NOT NULL, "username" text NOT NULL, PRIMARY KEY ("user_id") , FOREIGN KEY ("user_id") REFERENCES "public"."users"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("user_id"), UNIQUE ("username"));
