projectName: secretariat
ciNamespace: ci-secretariat

dependencies:
  fabrique:
    dependencies:
      contrib:
        preDeploy:
          importSecrets:
            options:
              secrets:
                pg-admin-user:
                  enabled: false
    extends:
    - name: ovh
      ifEnv: [dev,preprod]
    - name: buildkit-service
      ifEnv: [dev,preprod]
